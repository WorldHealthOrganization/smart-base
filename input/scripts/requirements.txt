# Python dependencies for SMART Guidelines DAK extraction scripts
# Install with: pip install -r requirements.txt

# PDF processing for personas extraction (extractpr.py)
pdfplumber>=0.11.0

# Data processing for Excel files (dd_extractor.py, dt_extractor.py, req_extractor.py, isco08_extractor.py)
pandas>=2.0.0
openpyxl>=3.1.0

# XML processing and transformations (installer.py)
lxml>=6.0.0

# YAML configuration processing (installer.py)
PyYAML>=6.0.0

# HTTP requests for PR comment scripts (pr_comment_start.py, pr_comment_finish.py)
requests>=2.31.0

# Babel: i18n/l10n support — used by extract_script_strings.py to extract
# translatable strings from Python source files into .pot templates.
Babel>=2.10.0

# AI-powered .po translation fill-in (pull_gpt_translations.py).
# Supports OpenAI, Anthropic/Claude, DeepSeek, and Azure OpenAI.
# Only active when a provider API key env var is set (OPENAI_API_KEY, etc.).
gpt-po-translator>=1.6.0